Исправлена панель: кнопка «скорость для этого канала» могла лежать поверх закладки «по умолчанию везде» — при ползунке снизу и в узком окне при ползунке справа. Теперь это две отдельные кнопки, каждая на своём месте.

---

The panel is fixed: the "speed for this channel" button could sit on top of the "default everywhere" bookmark — with the slider below the buttons, and in a narrow window with the slider on the right. They are two separate buttons again, each in its own place.
